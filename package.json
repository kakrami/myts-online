{
  "name": "myts-gotsport-probe",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check worker.js && node --check inspection.js"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
